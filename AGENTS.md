# 🤖 AGENTS.md - Antigravity AI 프로젝트 개발 지침 및 헌법 규칙

> **본 파일은 AI 에이전트가 본 프로젝트에서 코드를 작성, 수정, 리팩터링할 때 매 턴마다 최우선으로 반드시 읽고 강제 준수해야 하는 최상위 프로젝트 지침(Constitution)입니다.**

---

## 🚨 [최우선 강제 헌법] 서비스 기능 기획서 준수 원칙

본 프로젝트의 모든 프론트엔드/백엔드 코드 수정, API 엔드포인트 변경, 신규 기능 추가 시에는 반드시 루트의 **[SERVICE_SPECIFICATION.md](SERVICE_SPECIFICATION.md)**에 정의된 기획 및 비즈니스 로직을 **100% 일치하도록 최우선 준수**해야 합니다.

임의로 단순화하거나 기존 서비스 기획 룰을 무시하고 일반적인 CRUD 방식으로 구현해서는 안 됩니다.

---

## 📌 필수 구현 및 유지 비즈니스 규칙 (Core Rules)

### 1. 홈 피드 (Home Feed) 3단계 노출 우선순위 & 시청 필터링
홈 피드 조회 API (`GET /api/posts/feed`) 및 프론트엔드 피드 렌더링 시 아래 규칙을 반드시 준수해야 합니다:
- **[1순위] 최근 등록한 내 게시물**:
  - 작성자 본인이 최근 등록한 게시물을 피드 최상단에 1회 최초 노출.
  - 단, 본인이 시청(확인)한 이후에는 피드 스트림에서 제외(숨김).
- **[2순위] 내가 아직 확인하지 않은 친구(팔로잉)의 미시청 게시물**:
  - 팔로우 승인된 친구들의 게시물 중 **내가 아직 보지 않은 게시물만** 최신순으로 우선 노출.
- **[3순위] 무작위(추천) 사람들의 미시청 공개 게시물 (피드 무한 연속성)**:
  - 확인하지 않은 친구 게시물이 없거나 소진되더라도 피드가 중간에 끊기지 않도록, 아직 보지 않은 전체 공개 게시물을 무한 스트림으로 연결.
- **[절대 규칙] 시청한 게시글 재노출 방지 (Seen Filter)**:
  - 사용자가 한 번이라도 확인/체류한 게시물(`content_views` 기록)은 1~3순위 전체에서 **절대 다시 피드에 노출되지 않아야 함**.

---

### 2. 24시간 스토리 (Stories)
- 등록 후 정확히 24시간 동안만 유효 (`expires_at > UTC NOW`).
- 비공개 계정의 스토리는 본인 및 승인된 팔로워에게만 노출.
- **링 시각화**: 미시청 스토리가 남아있으면 선셋 그라디언트 링, 모두 확인했으면 회색 링.
- 스토리 뷰어 진입 시 5초 자동 진행 및 `POST /api/stories/{id}/view` 호출로 읽음 처리.

---

### 3. 릴스 (Reels) 추천 및 중복 분산 알고리즘
- 9:16 세로형 풀스크린 스냅 스크롤 (`scroll-snap-type: y mandatory`).
- 시청 이력(`content_views`) 및 세션 내 `exclude_ids`를 조회 풀에서 배제.
- `_generate_cycle`을 통해 동일한 영상(`prev_vid`) 또는 동일한 작성자(`prev_user`)가 연달아 2회 연속 노출되지 않도록 인터리빙 분산 유지.

---

### 4. 1:1 다이렉트 메시지 (Direct Messages)
- 두 사용자 간에는 언제나 유일한 1개의 대화방(`Conversation`)만 유지 (`min(u1, u2)`, `max(u1, u2)`).
- 대화방 진입 시 상대방이 보낸 미확인 메시지는 즉시 `is_read = True` 일괄 처리.

---

### 5. 소셜 인터랙션 & UX 일관성
- 좋아요, 북마크는 낙관적 업데이트(Optimistic UI)로 즉각 반응.
- 비로그인 게스트가 상호작용(좋아요, 댓글, 작성, 팔로우 등)을 시도할 경우 작업을 차단하고 로그인 유도 모달 또는 로그인 페이지로 부드럽게 안내.

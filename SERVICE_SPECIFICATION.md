# 📱 Instagram 클론 서비스 기능 기획서 (SERVICE_SPECIFICATION.md)

> **문서 버전**: v1.0  
> **기준**: 현재 프로젝트 소스코드(React + FastAPI + SQLite/PostgreSQL)에 **실제로 구현된 기능만을 100% 기반**으로 정리한 서비스 기능 상세 기획서 (Product Functional Specification / PRD) 입니다.  
> **미구현된 가상 기능은 일체 배제**하고, 현재 동작 가능한 기능의 **사용자 플로우, 화면 요소, 비즈니스 규칙 및 노출 알고리즘**을 체계적으로 정의합니다.

---

## 📑 목차 (Table of Contents)
1. [서비스 개요 및 사용자 권한 체계](#1-서비스-개요-및-사용자-권한-체계)
2. [홈 피드 (Home Feed) 기능 명세](#2-홈-피드-home-feed-기능-명세)
3. [24시간 스토리 (Stories) 기능 명세](#3-24시간-스토리-stories-기능-명세)
4. [릴스 (Reels) 숏폼 기능 명세](#4-릴스-reels-숏폼-기능-명세)
5. [탐색 및 검색 (Explore & Search) 기능 명세](#5-탐색-및-검색-explore--search-기능-명세)
6. [소셜 인터랙션 및 팔로우 (Social & Follow) 기능 명세](#6-소셜-인터랙션-및-팔로우-social--follow-기능-명세)
7. [다이렉트 메시지 (Direct Message - 1:1 DM) 기능 명세](#7-다이렉트-메시지-direct-message---11-dm-기능-명세)
8. [프로필 및 계정 설정 (Profile & Settings) 기능 명세](#8-프로필-및-계정-설정-profile--settings-기능-명세)
9. [체류 시간 및 시청 분석 (Views & Dwell Time) 기능 명세](#9-체류-시간-및-시청-분석-views--dwell-time-기능-명세)
10. [알림 및 신고/관리자 거버넌스 (Notifications & Admin)](#10-알림-및-신고관리자-거버넌스-notifications--admin)
11. [공통 UX 및 접근 제어 정책](#11-공통-ux-및-접근-제어-정책)

---

## 1. 서비스 개요 및 사용자 권한 체계

### 1.1 서비스 정의
Instagram 고유의 감성과 핵심 소셜 미디어 경험(피드, 스토리, 릴스, DM, 탐색, 프로필)을 웹 환경에서 완벽하게 제공하는 풀스택 SNS 플랫폼입니다.

### 1.2 사용자 권한 분류
| 사용자 등급 | 식별 조건 | 접근 가능 범위 | 제한 사항 |
|---|---|---|---|
| **비로그인 게스트 (Guest)** | JWT 토큰 없음 | - 공개 홈 피드 열람<br/>- 공개 계정 탐색(Explore) 및 릴스 시청<br/>- 공개 사용자 프로필 조회 | - 좋아요, 댓글, 북마크 불가<br/>- 글/스토리/릴스 작성 불가<br/>- DM 및 팔로우 불가<br/>*(작업 시도시 로그인 유도 모달 팝업)* |
| **일반 회원 (User)** | 유효한 JWT Access Token 보유 | - 모든 소셜 기능(작성, 좋아요, 댓글, 북마크, 팔로우)<br/>- 1:1 실시간 DM 대화<br/>- 스토리 시청 및 업로드<br/>- 프로필 수정 및 본인 콘텐츠 관리 | - 관리자 대시보드 접근 불가<br/>- 타인 게시물/댓글 강제 삭제 불가 |
| **관리자 (Admin)** | `User.is_admin == True` | - 일반 회원 기능 전체<br/>- 관리자 전용 대시보드 (`/admin`)<br/>- 서비스 지표 통계 열람<br/>- 악성 유저 차단/해제<br/>- 신고 접수 내역 검토 및 콘텐츠 강제 삭제 | - |

---

## 2. 홈 피드 (Home Feed) 기능 명세

### 2.1 피드 노출 및 정렬 알고리즘 (3단계 우선순위 & 시청 중복 제외)
1. **진입점**: 메인 URL (`/`)
2. **로그인 회원 피드 3단계 노출 우선순위**:
   - **[1순위] 최근 업로드한 나의 새 게시물**:
     - 내가 방금/최근 등록한 게시물이 피드 최상단에 1회 최초 노출.
     - 단, 내가 내 게시물을 확인(시청)한 이후에는 피드 스트림에서 숨김 처리됨.
   - **[2순위] 내가 아직 확인 못한 친구(팔로잉)의 미시청 게시물**:
     - 내가 팔로우한 사용자들(`Follow.status == 'accepted'`)의 게시물 중, **내가 아직 보지 않은(미확인) 게시물**만 최신순으로 우선 노출.
     - **중복 노출 방지(Deduplication)**: 내가 이미 한 번이라도 확인(시청/체류 기록 발생)한 친구의 게시물은 피드에서 다시 나오지 않음.
   - **[3순위] 무작위(알고리즘 추천) 사람들의 미시청 공개 게시물 (피드 무한 연속성)**:
     - 확인하지 않은 친구의 게시물을 모두 소진했거나 피드가 끊기지 않도록, 전체 공개 계정 중 **내가 아직 보지 않은 다른 사람들의 게시물**을 무한 스트림으로 연결하여 끊김 없는 피드 제공.
   - **핵심 시청 필터 공통 원칙 (Seen Filter)**:
     - **내가 한 번 본 게시물은 피드에 절대 다시 나오지 않음**: `content_views` 테이블(체류 시간 및 시청 로그)을 대조하여 이미 열람한 게시물 ID는 1~3순위 전체 후보군에서 완벽히 배제.
   - **페이징 및 스트리밍**:
     - 커서/오프셋 기반 무한 스크롤을 통해 게시물이 중간에 끊기지 않고 연속으로 로딩되도록 구현.
3. **비로그인 게스트 피드**:
   - 공개 계정(`User.is_private == False`)의 최신 게시물을 역순으로 노출 (클라이언트 세션에서 본 글 제외).

### 2.2 피드 카드 UI 및 인터랙션 정책
- **헤더**:
  - 작성자 아바타 (스토리 보유 시 그라디언트 테두리), 사용자명(Username), 작성 시간 표기(예: `3시간 전`), 위치 태그(있을 경우), 옵션 메뉴(`···`).
- **다중 미디어 캐러셀 (Media Carousel)**:
  - 이미지/비디오 1~N개 지원.
  - 슬라이드 좌/우 화살표 버튼 및 하단 도트 인디케이터(현재 위치 표시).
  - 마우스 드래그 및 터치 스와이프 탐색 지원.
- **더블 탭 하트 (Double-Tap Heart) 인터랙션**:
  - 미디어 영역을 300ms 이내에 더블 클릭/더블 탭하면 화면 중앙에 흰색 하트 아이콘이 팝업되며 확대 후 서서히 사라짐.
  - 동시에 게시물 좋아요가 활성화(빨간 하트)됨 (이미 좋아요 상태인 경우 좋아요 유지).
- **낙관적 업데이트 (Optimistic UI)**:
  - 좋아요(❤️) 및 북마크(🔖) 버튼 클릭 시, 서버 응답을 기다리지 않고 UI 상태 및 카운터를 즉시 토글 반영. API 실패 시 롤백.
- **댓글 영역**:
  - 작성자 캡션(더보기 버튼 지원).
  - 최근 댓글 미리보기 노출 (댓글 2개 이상일 경우 "댓글 N개 모두 보기" 링크 제공).
  - 인라인 빠른 댓글 입력창 (Enter 또는 "게시" 클릭 시 즉시 추가).

---

## 3. 24시간 스토리 (Stories) 기능 명세

### 3.1 상단 스토리 트레이 (Story Tray) 노출 정책
1. **위치**: 홈 피드 상단 수평 스크롤 영역.
2. **24시간 자동 만료 규칙**:
   - `Story.expires_at > UTC NOW` 조건만 노출 (등록 시점으로부터 정확히 24시간 후 자동 제외).
3. **비공개 계정 열람 제한**:
   - 작성자가 비공개 계정인 경우, 본인이거나 팔로우 승인된 유저에게만 스토리 트레이에 노출.
4. **시청 여부(Viewed)에 따른 시각적 링 차별화**:
   - **안 본 스토리가 있는 경우 (`has_unseen == True`)**: 인스타그램 시그니처 45도 선셋 그라디언트 링(오렌지-핑크-퍼플) 표시.
   - **모든 스토리를 이미 확인한 경우 (`has_unseen == False`)**: 모노톤 회색 링 표시.
5. **내 스토리 영역**:
   - 트레이 최좌측 첫 번째 항목은 항상 '내 스토리' 전용 영역.
   - 스토리가 없으면 `+` 아이콘이 표시되어 클릭 시 스토리 업로드 모달 오픈.

### 3.2 전체화면 스토리 뷰어 (Story Viewer Modal)
1. **자동 진행 타이머**:
   - 상단에 유저의 스토리 개수만큼 프로그레스 바 분할 배치.
   - 각 스토리마다 **5초 동안 진행 게이지**가 차오르며, 5초 완료 시 다음 스토리로 자동 전환.
2. **수동 탐색 컨트롤**:
   - 화면 좌측 영역 탭/클릭: 이전 스토리로 이동.
   - 화면 우측 영역 탭/클릭: 다음 스토리로 스킵.
   - 마우스 호버 또는 길게 누르기(Long Press): 타이머 일시 정지(Pause).
3. **시청 기록 처리**:
   - 스토리가 뷰어에 렌더링되는 즉시 백엔드 `POST /api/stories/{story_id}/view` 호출.
   - `story_views` 테이블에 유저 ID 및 시청 시각 기록 → 트레이 링 색상이 회색으로 갱신됨.

---

## 4. 릴스 (Reels) 숏폼 기능 명세

### 4.1 수직 스냅 비디오 플레이어
1. **뷰포트**: 9:16 비율 풀스크린 숏폼 비디오.
2. **스크롤 UX**: CSS `scroll-snap-type: y mandatory` 기반으로 한 번 스크롤할 때마다 다음 릴스로 정확히 1개씩 스냅 전환.
3. **재생 정책**:
   - 현재 화면 중앙 뷰포트에 50% 이상 위치한 영상만 자동 재생(Auto-play).
   - 뷰포트를 벗어난 이전/다음 영상은 즉시 일시 정지(Pause) 및 리셋되어 리소스 절약.
   - 화면 탭 시 비디오 일시정지/재생 토글.
   - 우측 상단 글로벌 음소거/사운드 켜기 토글.

### 4.2 릴스 추천 스트림 및 중복 방지 알고리즘 (코드 구현 상세)
- **시청한 릴스 필터링**:
  - 클라이언트 세션에서 이미 노출된 `exclude_ids` + DB `content_views`에 기록된 유저의 기시청 릴스 ID를 조회 풀에서 제외.
- **연속 노출 분산 알고리즘 (Deduplication & Dispersion)**:
  - `_generate_cycle` 로직을 통해 **동일한 영상(`prev_vid`)** 또는 **동일한 작성자(`prev_user`)**가 연달아 2번 연속으로 나오지 않도록 인터리빙(Interleaving) 셔플 수행.
- **60초 인메모리 메타데이터 캐싱**:
  - 빠른 무한 스크롤 반응 속도를 위해 릴스 메타데이터를 60초간 메모리에 캐싱하여 DB 부하 방지.

### 4.3 릴스 우측 액션 바
- **작성자 정보**: 아바타, 사용자명, 팔로우 버튼 (내가 이미 팔로우한 유저면 '팔로잉' 표시).
- **인터랙션 버튼**:
  - 좋아요(❤️): 좋아요 카운터 표시, 토글 애니메이션.
  - 댓글(💬): 클릭 시 화면 우측/하단에서 릴스 댓글 서랍(Drawer) 슬라이드 오픈.
  - 북마크(🔖): 저장됨 보관함에 저장/취소.
  - 오디오 바(🎵): 사용된 배경음악 제목 및 회전하는 앨범 커버 애니메이션.

---

## 5. 탐색 및 검색 (Explore & Search) 기능 명세

### 5.1 탐색(Explore) 비대칭 3열 그리드
1. **진입점**: 좌측 내비게이션 / 모바일 하단 탭의 '탐색' (`/explore`).
2. **레이아웃 구조**:
   - 인스타그램 고유의 반응형 3열 그리드.
   - 일반 정사각형(1x1) 아이템과 세로형 대형 아이템(릴스 등)이 섞인 비대칭 모자이크 패턴.
3. **호버 인터랙션 (Desktop)**:
   - 마우스 마우스오버 시 어두운 딤(Dim) 오버레이와 함께 **좋아요 수(❤️) 및 댓글 수(💬)** 오버레이 표시.
4. **상세 모달**:
   - 썸네일 클릭 시 주소 이동 없이 단일 게시물 상세 모달 팝업.

### 5.2 사용자 검색 (Search Drawer)
1. **검색 입력**: 사용자명(Username) 또는 이름(Full Name) 키워드 검색.
2. **디바운스(Debounce)**: 불필요한 API 요청을 방지하기 위해 300ms 입력 지연 적용 후 백엔드 `/api/users/search` 쿼리 실행.
3. **결과 노출**: 일치하는 사용자의 프로필 사진, 유저네임, 본명, 팔로워 수가 표시되며 클릭 시 해당 프로필 페이지로 이동.

---

## 6. 소셜 인터랙션 및 팔로우 (Social & Follow) 기능 명세

### 6.1 팔로우 정책 및 계정 보안 타입
- **공개 계정 (Public Account)**:
  - 팔로우 클릭 즉시 `status = 'accepted'`로 체결.
  - 내 피드 및 스토리에 상대방 콘텐츠가 즉시 포함됨.
- **비공개 계정 (Private Account)**:
  - `User.is_private == True`인 계정에 팔로우 요청 시 `status = 'pending'`(요청됨) 상태로 저장.
  - 상대방이 승인하기 전까지는 피드, 스토리, 상세 게시물 목록 열람 차단.
- **맞팔로우(Mutual Follow) 판별**:
  - A가 B를 팔로우하고 동시에 B가 A를 팔로우한 경우 상호 맞팔 상태로 플래그 처리되어 추천 및 다이렉트 메시지에서 우선순위 반영.

### 6.2 게시물 작성 모달 (Create Post Modal)
1. **진입 경로**: 좌측 메뉴 '만들기' 버튼 클릭.
2. **3단계 작성 파이프라인**:
   - **1단계 (파일 업로드)**: 이미지/비디오 드래그 앤 드롭 또는 파일 탐색기 선택 (최대 10개).
   - **2단계 (비율 및 미리보기)**: 원본, 1:1 정방형, 4:5 세로형 비율 크롭 선택.
   - **3단계 (세부 정보 입력)**: 캡션(본문), 위치 태그 입력, 댓글 기능 해제 여부(`comments_disabled`), 좋아요 수 숨기기(`hide_like_count`) 토글.
3. **공유 완료**: 게시물 업로드 성공 시 피드 최상단에 새 게시물이 즉시 반영되며 모달 종료.

---

## 7. 다이렉트 메시지 (Direct Message - 1:1 DM) 기능 명세

### 7.1 대화방 관리
1. **1:1 유일 대화방 보장**:
   - `user1_id < user2_id` 제약조건을 통해 두 유저 간에는 언제나 고유한 1개의 대화방(`Conversation`)만 생성/유지.
2. **대화방 목록 정렬**:
   - 가장 최근에 메시지가 오간 대화방이 최상단에 위치 (`Conversation.updated_at DESC`).
3. **안 읽은 메시지(Unread Badge)**:
   - 상대방이 발송한 메시지 중 `is_read == False`인 개수를 집계하여 대화방 리스트 및 좌측 메뉴 DM 아이콘에 배지 카운터 표시.
   - 대화방을 클릭하여 진입하는 순간 모든 메시지가 `is_read = True`로 일괄 갱신됨.

### 7.2 메시지 기능
- **텍스트 및 미디어 전송**: 메시지 입력창에서 엔터 시 즉시 말풍선 추가.
- **하트 리액션(❤️)**: 수신된 말풍선을 더블 클릭하거나 호버 시 하트 이모지 리액션을 달거나 해제할 수 있음.
- **대화 상대 상태 표기**: 상대방의 스토리 등록 여부(스토리 링 표시) 및 프로필 요약 노출.

---

## 8. 프로필 및 계정 설정 (Profile & Settings) 기능 명세

### 8.1 프로필 페이지 (`/:username`)
1. **헤더 정보**: 프로필 사진, 사용자명, 게시물 수, 팔로워 수, 팔로잉 수, 이름, 소개글(Bio), 웹사이트 링크.
2. **버튼 액션**:
   - **본인 계정인 경우**: [프로필 편집], [설정(다크모드/로그아웃)] 버튼 노출.
   - **타인 계정인 경우**: [팔로우 / 팔로잉 / 요청됨], [메시지 보내기] 버튼 노출.
3. **콘텐츠 3대 탭**:
   - **게시물 탭**: 내가 올린 사진/동영상 3열 그리드.
   - **릴스 탭**: 내가 올린 릴스 숏폼 목록 (조회수 뱃지 포함).
   - **저장됨 탭 (본인만 열람 가능)**: 내가 북마크한 게시물 컬렉션.

### 8.2 프로필 편집 (`/accounts/edit`)
- 프로필 사진 변경 (이미지 파일 업로드 시 즉시 정적 스토리지 저장 및 갱신).
- 이름(Full Name), 소개(Bio), 웹사이트 URL, 비공개 계정 여부(`is_private`) 전환 설정.

---

## 9. 체류 시간 및 시청 분석 (Views & Dwell Time) 기능 명세

### 9.1 피드/릴스 체류 시간 측정
1. **데이터 수집 API**: `POST /api/views`
2. **완독/완주(Completed) 판별 기준**:
   - **일반 게시물**: 화면 내에서 **3,000ms(3초) 이상 머물렀을 때** 완독(`completed = True`)으로 자동 판별.
   - **릴스 비디오**: 영상 길이 대비 시청 비율(`watch_ratio`)이 1.0(100%)을 넘기면 완주 처리.
3. **부정 시그널 수집**:
   - 사용자가 '관심 없음' 버튼 클릭 시 `not_interested = True`로 저장되어 향후 추천 셔플에서 제외 대상이 됨.

---

## 10. 알림 및 신고/관리자 거버넌스 (Notifications & Admin)

### 10.1 소셜 알림 트리거 정책
- **내 게시물/댓글에 타인이 좋아요를 눌렀을 때** → 알림 생성 (`like`)
- **내 게시물에 타인이 댓글/대댓글을 달았을 때** → 알림 생성 (`comment`)
- **타인이 나를 팔로우했을 때** → 알림 생성 (`follow`)
- **예외 필터**: 본인이 본인의 글에 좋아요를 누르거나 본인 글에 댓글을 단 경우 알림을 생성하지 않음.
- **알림 확인**: 알림 드로어를 열면 `is_read = True` 처리.

### 10.2 신고(Report) 및 관리자 대시보드 (`/admin`)
- **신고 접수**: 부적절한 게시물, 릴스, 댓글을 신고 접수 (`/api/reports`).
- **관리자 전용 기능**:
  - 총 유저 수, 총 게시물 수, 총 릴스 수, 일일 활동 지표 카드.
  - 신고된 유해 콘텐츠 검토 후 즉시 삭제 처리.
  - 불량 이용자 계정 상태 변경 (비활성화/정지).

---

## 11. 공통 UX 및 접근 제어 정책

### 11.1 테마 모드 (Dark / Light Mode)
- `ThemeContext`를 통해 시스템 환경설정 감지 및 수동 토글 지원.
- `[data-theme='dark']` 속성을 최상위 `<html>` 태그에 주입하여 CSS Variable 디자인 토큰 전역 전환 (블랙 배경, 화이트 텍스트, 다크 보더).

### 11.2 비로그인 게스트 가드 (Auth Interceptor)
- 비로그인 상태로 좋아요, 북마크, 댓글 입력, 스토리 업로드, 팔로우를 시도할 경우 작업을 차단하고 **"로그인이 필요한 서비스입니다" 안내와 함께 로그인 모달 또는 `/login` 화면으로 부드럽게 유도**.

### 11.3 반응형 분기점 (Responsive Viewports)
- **데스크톱 (1024px 이상)**: 좌측 너비 244px 확장형 사이드바 + 중앙 피드(최대 630px) + 우측 추천 사이드바(320px).
- **태블릿 (768px ~ 1023px)**: 좌측 사이드바가 아이콘 전용 축소형(72px)으로 전환.
- **모바일 (767px 이하)**: 좌/우 사이드바 숨김, 상단 인스타그램 로고 헤더 + 하단 5대 고정 탭바로 레이아웃 자동 변환.
